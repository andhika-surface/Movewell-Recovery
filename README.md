# MoveWell Totalitas — Full Multi-page Elevated Build

This package includes a full multi-page elevated design (glassmorphism, gradients, parallax) with bilingual support (EN default, ID toggle).

Files:
- index.html, services.html, about.html, promo.html, contact.html
- css/style.css
- js/main.js
- sitemap.xml, README.md, .gitignore

Preview locally:
- Extract and open index.html OR run `python -m http.server` and open http://localhost:8000

Notes:
- Language preference stored in localStorage.
- Images external (Unsplash). Replace with local assets in /assets if desired.
